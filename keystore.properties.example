# Copy to keystore.properties (gitignored) for local/Play-signed release builds.
# Never commit keystore.properties, *.jks, or *.keystore.

storeFile=upload-keystore.jks
storePassword=REPLACE_ME
keyAlias=upload
keyPassword=REPLACE_ME
